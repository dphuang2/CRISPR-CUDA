rai:
  version: 0.2
  image: balphi/crispr-cuda:4
resources:
  cpu:
    architecture: amd64
  gpu:
    count: 1
commands:
  build:
    - cp -r /src .
    - cmake /src
    - make
    #-  nvprof -f --analysis-metrics -o timeline.nvvp ./crispr-cuda
    -  ./crispr-cuda
